#ifndef HLED_LCFG_H
#define HLED_LCFG_H

typedef enum
{
    LED_START,
    LED_STOP,
    LED_COUNT, /* this enum keeps track of led count (must always be at the end) */
} HLED_enuUserDefinedLEDNames_t;

#endif